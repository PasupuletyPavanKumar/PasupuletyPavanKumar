import React from "react";

const Dashboard = () => {
  return (<div><div>Dashboard is under development</div>
            <Button
        variant="primary"
        type="button"
        className="submit-button"
        onClick={submitLogin}
      >
        Logout
      </Button>            
</div>);
};

export default Dashboard;
